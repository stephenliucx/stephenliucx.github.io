## Creating a static website using Hugo

## Edits
1. `cd` to where it should be; `git pull`.
2. edit locally.
3. `hugo server [--BuildDrafts]` to preview.
4. `hugo` to deploy.
5. Finally, `git commit -m <Message>` and `git push`. Then github will deploy the website, which can be checked from the tab "Actions".
